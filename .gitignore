node_modules/
dist/
build/
.env
# Every .env variant and backup — a plain `.env` rule does not cover
# .env.local, .env.production, or a timestamped .env.bak.*, and `git add .`
# will happily commit the secrets in them.
.env.*
!.env.example
logs/
.DS_Store
.idea/
.vscode/
*.log
*.tsbuildinfo
coverage/
kafka-schemas/node_modules/
kafka-schemas/dist/
kafka-schemas/build/
apps/whatsapp-gateway/.wwebjs_auth/
apps/whatsapp-gateway/.wwebjs_cache/
scripts/sandbox/.sandbox-state.json
