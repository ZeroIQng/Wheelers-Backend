POSTGRES_DB=wheelers
POSTGRES_USER=postgres
# Avoid raw '$' here unless you escape it as '$$' for Docker Compose.
POSTGRES_PASSWORD=change-me
PORT=3000
JWT_SECRET=replace-with-at-least-32-random-characters
